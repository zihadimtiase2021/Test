app = "apnar-app-name" # Fly.io te je nam diyechen seta ekhane hobe
primary_region = "sin" # Singapore (Kachakachi region)

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
